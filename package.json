{
  "name": "virtualized-data-table",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "mock-api": "json-server --watch db.json --port 3001",
    "mock-api:docker": "json-server --watch /db.json --host 0.0.0.0 --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
